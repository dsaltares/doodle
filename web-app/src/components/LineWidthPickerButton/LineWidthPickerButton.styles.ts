import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    flex: 1,
    width: 200,
    height: 25,
    marginTop: 18,
  },
}));

export default useStyles;
